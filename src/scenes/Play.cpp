#include "Play.hpp"

namespace scene
{
    Play::Play()
    {
    }

    void Play::handleEvent(const sf::Event& event, const sf::Vector2f& mousePosition)
    {
    }

    void Play::update(const sf::Time& frameTime)
    {
    }

    void Play::draw(sf::RenderTarget &target, sf::RenderStates states) const
    {
    }
}